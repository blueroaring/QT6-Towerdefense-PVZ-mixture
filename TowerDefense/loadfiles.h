#ifndef LOADFILES_H
#define LOADFILES_H
#include <QJsonObject> // { }
#include <QJsonArray> // [ ]
#include <QJsonDocument> // 解析Json
#include <QJsonValue> // int float double bool null { } [ ]
#include <QJsonParseError>
class LoadFiles
{
public:
    LoadFiles();
};

#endif // LOADFILES_H
